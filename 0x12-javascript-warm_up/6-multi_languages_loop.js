#!/usr/bin/node
const loops = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let index = 0; index < loops.length; index++) {
  const loopToPrint = loops[index];
  console.log(loopToPrint);
}
